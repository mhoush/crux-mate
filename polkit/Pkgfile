# Description: An application-level toolkit for defining and handling privilege policies
# URL: https://www.freedesktop.org/wiki/Software/polkit/
# Maintainer: Matt Housh, jaeger at crux dot ninja
# Depends on: glib intltool nspr spidermonkey gobject-introspection

name=polkit
version=0.113
release=1
source=(http://www.freedesktop.org/software/$name/releases/$name-$version.tar.gz)

build() {
	cd $name-$version
	./configure --prefix=/usr \
		--disable-tests \
		--disable-nls \
		--disable-static \
		--with-polkit-user=polkit \
		--with-polkit-group=polkit \
		--with-authfw=shadow \
		--disable-man-pages
	make
	make DESTDIR=$PKG install

	rm -r $PKG/usr/share/locale
}
